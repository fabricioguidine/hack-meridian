# Deployed poap_badge contract id (strkey, starts with C)
CONTRACT_ID=CXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
# Soroban RPC endpoint
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
# HTTP port
PORT=4000
# Optional log filter
RUST_LOG=info
